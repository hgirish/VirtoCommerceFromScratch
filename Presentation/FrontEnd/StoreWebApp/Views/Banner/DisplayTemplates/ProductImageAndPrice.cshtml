@using StoreWebApp.Models
@{
    Layout = null;
	var currentModel = ViewData["Model"] as ProductWithImageAndPriceModel;
}

@if (currentModel != null)
{
    <p>@currentModel.ProductCode</p>
    @Html.Action("DisplayItemNoCache", "Catalog", new { itemId = currentModel.ProductCode, bycode = true, displayOptions = ItemDisplayOptions.ItemMedium, name = "DisplayTemplates/Item" })
}

